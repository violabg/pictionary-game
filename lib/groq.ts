"use server";

// This file is deprecated - use Convex actions instead
// Card generation is now handled by convex/actions/generateCards.ts
// Call the Convex action directly:
// const cardIds = await generateCards({ gameId, category, difficulty, playersCount });

export {};
