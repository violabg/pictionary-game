"use server";

// import { createClient } from "./client"; // Import when first function is added

// Add drawing-related Supabase functions here in the future
// For example:
// export async function saveDrawingSegment(...) {
//   const supabase = createClient(); // Initialize client within functions or at module scope if used by multiple functions
//   ...
// }
// export async function getDrawingSegments(...) { ... }
